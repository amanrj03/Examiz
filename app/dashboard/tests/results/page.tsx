import TestList from '@/components/dashboard/TestList';
export default function ResultsPage() {
  return <TestList filter="results" title="Results" subtitle="Tests whose window has ended — view submissions and analyse." />;
}
