import TestList from '@/components/dashboard/TestList';
export default function DraftsPage() {
  return <TestList filter="drafts" title="Drafts" subtitle="Tests saved as drafts — not yet published." />;
}
