import TestList from '@/components/dashboard/TestList';
export default function AllTestsPage() {
  return <TestList filter="all" title="All Tests" subtitle="All published tests — live, offline, and ended." />;
}
